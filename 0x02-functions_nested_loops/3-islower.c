#include "main.h"

/**
 * _islower - Entry point
 * Description: To check for lowercase.
 * @c: The parameter to check and verify
 * author: Muawiya
 * date:23-06-2023
 * Return: 0 (success)
*/

int _islower(int c)
{
	if (c >= 'a' && c <= 'a')
		return (1);
	else
		return (0);
}
